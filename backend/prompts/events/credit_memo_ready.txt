
CREDIT MEMO GATE: $role_count distinct specialist roles have completed ($role_text). You MUST call `present` with `type: "document"`, the exact title `"$title"`, and six items in this order: $sections. Each item must have a nonempty `source` string. Include DSCR/LTV/CIC, the three legal pillars with lane and assessment, proposed terms, and a recommendation grounded in the approval matrix. Item 5 must include `reason_codes` selected only from the runtime taxonomy in the system prompt. Top-level `sources` must list the tools and roles used.
